개발 PC 쪽 작업
-----------------------------------------------------
### 루트 디렉터리에 .gitignore 파일을 만들고, 그 안에 .env 입력 후 저장

### VS Code 에서 작업 폴더 열기 (작업 디렉토리로 이동)
cd "C:\Users\jungkwanpark.FAREAST\OneDrive - Microsoft\Desktop\App 실습\ㅍㅈㅅㅇㄷ\Microsoft Korea CSS - AI V-Team 활동\semantic_kernel"

### VS Code 로 파이썬 가상환경 새로 생성 (Select Dependencies to install => requirements.txt 부분은 무시!!!)

### VS Code 로 가상환경 접속 (.venv)

### Git이 관리할 수 있는 로컬 저장소 만들기 (.git 폴더가 없는 경우에만 실행)
git init

### 현재 디렉토리와 하위 디렉토리의 모든 변경사항(신규/수정/삭제된 파일)을 스테이징 영역에 추가
git add .

### 추가된 파일들을 로컬 저장소에 기록(Commit)
git commit -m "1st Commit"

### 원격 저장소 연결
git remote add origin https://github.com/jungkwanpark/project2.git

### 기본 브랜치 이름 생성/변경 (권장)
git branch -M main

### 원격 저장소로 PUSH
git push -u origin main
-----------------------------------------------------


우분투 리눅스 서버쪽 작업
-----------------------------------------------------
### apt 패키지 목록 업데이트 및 python3 등 관련 패키지 설치
sudo apt update && sudo apt install python3 && sudo apt install python3-pip
python3 --version

### git 설치
sudo apt install git
git --version

### git clone 명령어로 프로젝트를 복제
git clone https://github.com/jungkwanpark/project2.git

### 생성된 프로젝트 디렉터리로 이동
cd project2
ls -la

### requirements.txt 파일 설치 (의존성 설치)
cat requirements.txt
pip3 install -r requirements.txt

### .env 파일에 API Key 값들 입력
sudo vim .env
i
(복붙)
:wq


(### app.py 파일 실행
streamlit run ./demo/app_semantic_kernel.py)

(((### streamlit / .dotenv / pypdf / semantic-kernel 설치
pip3 install streamlit python-dotenv pypdf semantic-kernel)))


### 원격 클라이언트에서 Agent 서버 접속
http://your-server-domain:8501



★★★ PDF 파일 업로드 - 로컬 PC 폴더 위치
C:\Users\JungkwanPark

마이크로소프트의 FY 4Q25 실적은?
마이크로소프트 주식의 상승 여력은?
Intelligent Cloud 사업부 매출은?
-----------------------------------------------------
